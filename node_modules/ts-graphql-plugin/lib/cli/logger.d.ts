export interface Logger {
    debug(...args: string[]): void;
    info(...args: string[]): void;
    error(...args: any[]): void;
}
export declare type LogLevel = 'silent' | 'error' | 'info' | 'debug';
export declare class ConsoleLogger implements Logger {
    logLevel: LogLevel;
    constructor(logLevel?: LogLevel);
    error(...args: any[]): void;
    info(...args: string[]): void;
    debug(...args: string[]): void;
}
