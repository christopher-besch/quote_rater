import { SchemaManager } from './schema-manager';
import { SchemaManagerHost } from './types';
import { RequestSetup } from './request-introspection-query';
export declare class HttpSchemaManager extends SchemaManager {
    protected _options: RequestSetup | null;
    private _schema;
    constructor(_host: SchemaManagerHost, _options?: RequestSetup | null);
    protected _getOptions(): Promise<RequestSetup | null>;
    protected _fetchErrorOcurred(): void;
    getBaseSchema(): any;
    waitBaseSchema(): Promise<import("graphql").GraphQLSchema | null>;
    startWatch(interval?: number): void;
}
