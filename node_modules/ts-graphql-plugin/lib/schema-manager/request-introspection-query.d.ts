import { GraphQLSchema } from 'graphql';
export interface RequestSetup {
    url: string;
    method?: string;
    headers?: {
        [key: string]: string;
    };
}
export declare function isRequestSetup(requestSetup: RequestSetup | any): requestSetup is RequestSetup;
export declare function requestIntrospectionQuery(options: RequestSetup): Promise<GraphQLSchema>;
