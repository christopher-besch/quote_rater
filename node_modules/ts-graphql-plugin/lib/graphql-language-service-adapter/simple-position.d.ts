import { IPosition } from 'graphql-language-service-types';
export declare class SimplePosition implements IPosition {
    line: number;
    character: number;
    setLine(v: number): void;
    setCharacter(v: number): void;
    constructor(lc: ts.LineAndCharacter);
    lessThanOrEqualTo(p: IPosition): boolean;
}
