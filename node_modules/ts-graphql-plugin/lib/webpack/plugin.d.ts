import { Compiler } from 'webpack';
import { GetTransformerOptions } from '../transformer';
declare type WatchFileSystemCompiler = Compiler & {
    watchFileSystem: {
        watcher: {
            mtimes?: {
                [name: string]: number;
            };
        };
        wfs: {
            watcher: {
                mtimes?: {
                    [name: string]: number;
                };
            };
        };
    };
};
export declare class WebpackPlugin {
    private readonly _host;
    private _disabled;
    constructor({ tsconfigPath }?: {
        tsconfigPath?: string;
    });
    getTransformer(options?: GetTransformerOptions): (ctx: import("typescript").TransformationContext) => (sourceFile: import("typescript").SourceFile) => import("typescript").SourceFile;
    apply(compiler: WatchFileSystemCompiler): void;
}
export {};
