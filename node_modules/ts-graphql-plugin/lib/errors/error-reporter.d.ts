import { TsGqlError, ErrorWithLocation, ErrorWithoutLocation } from '.';
export declare class ErrorReporter {
    private readonly _currentDirectory;
    private readonly _output;
    constructor(_currentDirectory: string, _output?: (msg: string) => void);
    outputError(error: TsGqlError): void;
    _outputErrorWithoutLocation(error: ErrorWithoutLocation): void;
    _outputErrorWithLocation(error: ErrorWithLocation): string;
}
